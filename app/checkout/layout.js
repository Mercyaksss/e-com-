export const metadata = {
  title: 'Checkout — Sole.',
  description: 'Complete your order securely. Fast delivery across Nigeria.',
};

export default function CheckoutLayout({ children }) {
  return <>{children}</>;
}